# KocSistem (monorepo)

The **TaskFlow** Next.js app is in the **`taskflow/`** folder.

**If you use Vercel:** set **Root Directory** to **`taskflow`** (not the repository root), then deploy.  
See `taskflow/README.md` for full steps. Opening the repo without that setting often gives **404** or a broken site.
